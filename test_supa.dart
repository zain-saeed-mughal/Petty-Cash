import 'package:flutter/foundation.dart';

void main() {
  debugPrint('Supabase diagnostic script disabled for safety.');
  debugPrint(
    'This script was replaced with a no-op to avoid accidental writes during analysis or CI runs.',
  );
}
